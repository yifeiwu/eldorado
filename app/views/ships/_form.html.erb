<%= form_for(ship) do |f| %>
  <% if ship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ship.errors.count, "error") %> prohibited this ship from being saved:</h2>

      <ul>
      <% ship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ship_name %>
    <%= f.text_field :ship_name %>
  </div>

  <div class="field">
    <%= f.label :ship_capacity %>
    <%= f.number_field :ship_capacity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
